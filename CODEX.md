# Codex Instructions

- At the start of a session, read `README.md`, `WORKLOG.md`, and `TODO.md`.
- Preserve each tool in its own folder under `tools/`.
- Update `WORKLOG.md` before ending a substantive session.
- Update `TODO.md` when task status changes.
- Keep deployment and setup steps in `README.md`.
- Prefer small commits with clear messages.

